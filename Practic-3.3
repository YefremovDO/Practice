#include <iostream>
#include <time.h>
#include <ctime>
#include "windows.h"
using namespace std;

int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	const int N = 10;
	int S = 0;
	int array[N];
	srand(time(NULL));

	for (int i = 0; i < N; i++)
	{
		array[i] = rand() % 8 + 14;
		if (!(array[i] % 2))
		{
			S += array[i];
		}
		cout << "N[" << i << "] = " << array[i] << endl;
	}

	cout << "Сумма = " << S << endl;

	return 0;
}

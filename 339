#include "stud.h"
#include <iostream>
#include <string>
#include "windows.h"
using namespace std;

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    Student* student;
    int size = 7, n, count = 0;

    student = new Student[size];

    //  Student_element(student, size);
    Gotovo(student, size);

    cout << "1. Впорядкований список по групах." << endl;
    New_list(student, size);
    cin >> n;
    cout << "2. Прізвища студентів за спаданням середнього балу. " << endl;
    Upor_list(student, size);

    return 0;
}
